=========================Frd_Db=====================
Explanation:
  now it is Zend Db,so you can take a look at zf1's Zend_Db document 
  some basic usage:
    $db=app()->getDb();

    or

    $db=app()->getDb("default");

    $db->query(SQL);
    $rows=$db->fetchAll(SQL);

    $select=$db->select();
    $select->....

    $rows=$db->fetchAll($select);

    //the WHERE should use db to quote
    $db->update(TABLE_NAME,array("title"=>"test2",WHERE)


